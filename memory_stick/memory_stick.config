# ── Memory Stick ─────────────────────────────────────────────
# Puerto donde MS levanta su servidor (para CPUs)
PORT=8003

# IP y puerto de Kernel Memory
KM_IP=127.0.0.1
KM_PORT=8002

# Nivel de log
LOG_LEVEL=INFO

# Delay en ms antes de responder lectura/escritura (CP3)
MEMORY_DELAY=1500